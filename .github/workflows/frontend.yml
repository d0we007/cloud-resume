name: frotend

on:
  workflow_dispatch:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl private --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: resume.miguel.cloud
        AWS_ACCESS_KEY_ID: AKIA6K2OAHLRECYLVPAQ
        AWS_SECRET_ACCESS_KEY: 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
        AWS_REGION: 'us-east-1'
        SOURCE_DIR: 'cloud-resume/frontend'

